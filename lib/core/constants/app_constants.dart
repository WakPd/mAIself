class AppConstants {
  static const Duration defaultAnimationDuration = Duration(milliseconds: 300);
  static const double lowGaugeThreshold = 30.0;
  static const double mediumGaugeThreshold = 60.0;
}
