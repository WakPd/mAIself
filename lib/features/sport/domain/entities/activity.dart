class Activity {
  final String type;
  final int durationMinutes;
  final int gaugeImpact;

  const Activity({
    required this.type,
    required this.durationMinutes,
    required this.gaugeImpact,
  });
}
